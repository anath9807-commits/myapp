name: Build APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install Cordova
        run: npm install -g cordova

      - name: Create App Structure
        run: |
          cordova create myapp com.vinay.myapp "Test Pro"
          # Dono files ko sahi jagah copy karna zaroori hai
          cp index.html myapp/www/
          cp config.xml myapp/
          cd myapp
          cordova platform add android
          cordova build android --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: My-App-Release
          # Wildcard (*) use kiya hai taaki koi bhi release APK mil jaye
          path: myapp/platforms/android/app/build/outputs/apk/release/*.apk
